{
  "name": "ghost-blog",
  "version": "1.0.0",
  "description": "Ghost blog on Render",
  "main": "index.js",
  "scripts": {
    "start": "ghost start --development",
    "build": "npm install -g ghost-cli@latest"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "ghost-cli": "^1.25.0"
  }
}